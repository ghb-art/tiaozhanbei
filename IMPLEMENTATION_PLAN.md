# DB4AI-EdgeServe P0-B1 收敛训练与最终验证计划

状态日期：2026-08-04

本文件同时定义后续实施约束和记录当前真实进度。计划中的“通过”只能以
`reports/audit/`下对应审计文件为准，训练日志或中间检查点不能替代正式审计。

## 0. 当前唯一执行主线：P0-B1

P0-A45至P0-A48的大混合训练和小规模专项修复均已结束，只保留为失败经验。
当前不再训练或部署三个任务模型，也不再根据正式测试逐题输出调整数据。
P0-B1最终只产生一个共享Qwen3-1.7B模型，并保持`Q4_K_M + Q8 KV`边侧路线。

### 0.1 数据与隔离

| 领域 | 训练集 | 训练内部验证 | 300题 | 正式全量 |
|---|---:|---:|---:|---:|
| Math | GSM8K train 7,173 | 200 | 100 | GSM8K test 1,319 |
| Code | OpenCodeInstruct严格函数题20,000 | 新留出300（总留出1,500） | 100 | HumanEval 164 |
| NLP | 中文四选一去重30,000 | 新留出300（总留出1,500） | 100 | CMMLU test 11,582 |

Code只保留HumanEval同形态的Python函数体任务，原答案和转换后的函数体都必须通过
10个单元测试。新增数据来自未使用的OpenCodeInstruct分片。HumanEval题面只用于自动
污染排除，不读取答案、测试或逐题模型结果。

NLP由当前10,178条高质量中文选择题和新增的MMLU auxiliary_train翻译复核数据组成。
14B Teacher看不到源标签；只有Teacher独立选择与锁定训练标签一致、中文字段完整的样本
才可接收。CMMLU test不会进入下载、生成、训练、内部验证或检查点选择。

训练数据只根据正式任务的汇总形态调整：Math为带推理的数值题，Code为短Python函数，
NLP为中文多学科四选一。禁止读取正式逐题错误来选样本或设置权重。

### 0.2 单一共享模型训练

- 基座：`models/checkpoints/p0a4/student-shared-merged`。
- LoRA：Rank 64、Alpha 128，覆盖全部注意力与MLP投影；合并后仍是一个1.7B模型。
- 最大4轮；每250步在800条训练内部验证上评估。
- 连续4次评估的`eval_loss`改善不足0.002时早停，训练结束恢复最佳检查点。
- Math/Code/NLP有效损失占比为30%/35%/35%，不通过简单复制样本实现平衡。
- 全任务都对禁用Adapter的v1基座做KL保持：Math 0.25，Code/NLP 0.12，温度2.0。
- 正则化：LoRA dropout 0.10、AdamW weight decay 0.05、label smoothing 0.02、
  最大梯度范数1.0、5% warmup和cosine学习率调度。
- 4张GPU做DDP训练；每个rank每个batch都执行基座参考前向，避免混合任务条件分支死锁。
- 内存守卫只监控系统RAM，阈值60%；训练支持完整检查点恢复。

配置与统一入口：

```text
configs/p0b1_converged_shared.json
scripts/run_p0b1.sh
```

### 0.3 收口顺序

```text
Code双重执行验证 + NLP 14B翻译复核
→ 57,173条共享训练集和800条内部验证
→ 大规模训练直至自然结束或早停
→ 恢复最佳检查点并合并LoRA
→ 纯训练数据imatrix
→ 单个Q4_K_M模型，Q8 KV
→ 一次300题测试
→ 不论300题结果，一次13,065题边侧全量测试
→ 仅输出三领域汇总准确率与保持率，不再据此训练
```

300题和正式全量都只能在训练完全停止、最佳检查点冻结、量化完成后运行。P0-B1正式
全量密封结果存在后，脚本拒绝覆盖或重复运行。

下文P0-A5/P0-A6内容作为历史协议和已有证据保留；与本节冲突时以P0-B1为准。

## 1. 项目目标

题目编号：XH-202606
题目名称：面向云边协同场景的分布式人工智能感知与决策关键技术研究

本项目建设“边缘实时感知与自治、云边协同推理、全局一致性决策”完整系统，并以可复现证据达到以下指标：

| 指标 | 正式要求 | 项目开发目标 |
|---|---:|---:|
| Math/Code/NLP能力保持率 | 每项80%–90% | 小门禁推荐线82%，正式每项≥80% |
| TTFT降低 | ≥75% | ≥78% |
| 单次推理内存 | ≤1500MB | ≤1400MB |
| 弱网业务保持率 | ≥90% | ≥92% |
| 两类场景平均端到端时延 | ≤200ms | ≤180ms |
| 决策冲突比例 | ≤5% | ≤4% |
| 冲突解决成功率 | ≥90% | ≥92% |

能力、内存、时延和系统一致性必须分别提供证据。任何开发门通过都不得写成正式比赛指标已经通过。

## 2. 当前有效模型角色

### 2.1 Baseline-14B-AWQ

- 模型：Qwen2.5-14B-Instruct-AWQ。
- 角色：冻结的云端工程基线和能力保持率分母。
- 禁止训练、禁止根据正式逐题错误调整提示词或训练数据。
- 已完成正式13,065题评测，结果与逐题记录继续封存。
- 报告同时补充BF16 Teacher的小规模一致性结果，解释AWQ作为工程基线的合理性。

### 2.2 Distill-Teacher-14B

- 模型：Qwen2.5-14B-Instruct BF16。
- 训练：4 GPU、DeepSpeed ZeRO-3、LoRA Rank 16、Alpha 32、学习率`1e-5`、1轮。
- CPU参数和优化器卸载继续禁用。
- 作用：生成和校验Math、Code、NLP训练信号，不作为正式分母。

### 2.3 Edge-Student

- 基座：当前表现最稳定的Qwen3-1.7B v1 HF合并模型。
- 方法：单一共享多任务LoRA，不再使用三个任务Adapter。
- 默认参数：Rank 16、Alpha 32、学习率`1e-5`、1轮。
- 最多两个预注册候选；第二候选只能降低学习率并加强Math保持约束。
- 最终量化：Q4_K_M权重、Q8_0 KV Cache、纯训练数据imatrix。

## 3. 唯一有效的能力数据协议

旧96题、170题、MBPP、APPS、CodeContests、MMLU翻译和任务Adapter协议全部退出主线。历史结果只作为失败路线证据，不参与新候选选择。

### 3.1 数据总表

| 领域 | 训练 | 训练内部验证 | 唯一小门禁 | 正式测试 |
|---|---|---|---|---|
| Math | GSM8K train 7,173题 | GSM8K train留出200题 | GSM8K train固定100题 | GSM8K test 1,319题 |
| Code | OpenCodeInstruct筛选20,000题 | 同源独立1,000题 | 同源独立100题 | HumanEval 164题 |
| NLP | COIG-CQIA分层9,500题 | 同源独立1,000题 | CMMLU dev固定100题 | CMMLU test 11,582题 |

内部验证只用于训练损失和检查点选择，不是能力门禁。正式测试不进入训练、蒸馏、内部验证、提示词选择或候选选择。

### 3.2 Math

- GSM8K官方train按照固定种子划分为7,173/200/100。
- 训练保留人工推理过程和最终数值答案。
- 数值答案通过`####`后的标准答案自动校验。
- GSM8K test 1,319题完全封存。

Math已接近达标，训练目标是防遗忘。共享Student的Math损失权重固定为30%，并对Math答案Token计算“启用LoRA模型”与“禁用LoRA的v1基座”之间的KL保持损失：

```text
Math loss = 0.5 × supervised loss + 0.5 × base-student KL
```

候选的Math保持率相对当前v1不得下降超过1个百分点。

### 3.3 Code

来源固定为`nvidia/OpenCodeInstruct`。只保留：

- Python短函数；
- 明确函数签名；
- 字符串、数组、字典、集合、排序、搜索、计数、基础贪心和动态规划；
- 完整单元测试；
- 数据集报告测试全部通过且平均测试分为1.0；
- 本项目独立沙箱再次执行通过；
- 与HumanEval不存在精确或高相似题面重叠。

排除：

- 网络、文件、GUI、数据库、外部服务和大型第三方依赖；
- 超长上下文和工程脚手架；
- 无法执行验证的开放式任务；
- 重复题面、仅改变量名或模板生成的重复任务。

Code正式测试仍为HumanEval 164题，执行时使用隔离进程、单候选pass@1和隐藏单元测试。可使用EvalPlus执行器强化同一164题的测试，但不得把HumanEval题目加入训练。

### 3.4 NLP

来源固定为`m-a-p/COIG-CQIA`。上游44,694条中只有12,726条标记
`human_verified=true`；执行长度、安全、去重、去CMMLU重叠和目标领域筛选后，
可用的不同样本为10,602条。因此不以未人工核验样本凑足20,000条，而是固定
9,500条训练、1,000条内部验证并保留102条安全余量。样本按互斥的学科优先级
确定性分层，避免一个多标签样本被重复计入多个类别：

| 类别 | 数量 |
|---|---:|
| 考试与试题 | 301 |
| 百科与自然科学 | 1,690 |
| 历史、文化与社会科学 | 1,246 |
| 法律、经济与管理 | 758 |
| 语言理解与逻辑推理 | 5,505 |

所有样本必须`human_verified=true`。排除代码、纯数学、闲聊、营销、主观创作、来源不清、超长样本和CMMLU重叠题。

Teacher只生成1–3句简短分析，人工核验的原答案保持不变：

```text
简短分析：……
最终答案：人工核验的参考答案
```

CMMLU dev固定100题覆盖全部67个学科，用于唯一小门禁；CMMLU test 11,582题只用于正式测试。

### 3.5 数据许可与提交

- OpenCodeInstruct：CC BY 4.0，记录数据版本和来源。
- COIG-CQIA数据卡当前未给出完整许可证结论；原始数据、筛选数据和逐题输出不进Git。
- Git只提交下载、筛选、去重、执行验证、哈希和重建脚本。
- 作品材料记录来源、使用范围和哈希；不擅自重新分发上游原始数据。

## 4. 训练流程

### 4.1 CPU准备

```text
下载3个分散的OpenCodeInstruct分片和COIG-CQIA
→ 构建三个训练集
→ 独立执行21,100个Code候选
→ 生成2,200条训练内部验证
→ 生成固定300题门禁
→ 数据重叠、数量、哈希和协议审计
→ Teacher/Student训练dry-run
```

入口：

```bash
bash scripts/run_p0a5.sh data-download
bash scripts/run_p0a5.sh data-build
bash scripts/run_p0a5.sh preflight
```

### 4.2 Teacher

```text
BF16 14B + 4 GPU ZeRO-3
→ 共享LoRA训练1轮
→ 启动4 GPU张量并行Teacher端点
→ 生成Math/Code/NLP蒸馏答案
→ Math数值校验、Code执行校验、NLP人工参考答案锁定
```

Teacher生成失败时，只允许回退到已经独立验证的原训练答案，并在审计中记录；不得接受未验证的生成内容。

Teacher训练固定配置：

```text
LoRA Rank 16 / Alpha 32 / Dropout 0.05
学习率 1e-5
最大序列长度 1536
4 GPU ZeRO-3，不卸载CPU参数或优化器
每50步保存一次检查点，只保留最近2个完整检查点
每100步在全部2,200条内部验证集上评估eval_loss
早停 patience=2，threshold=0.001
```

早停只比较内部验证损失，不读取300题门禁或正式测试结果。恢复旧检查点时先执行
一次完整内部验证，建立恢复点的基线；连续两次评估未改善至少0.001才提前停止。
训练自然完成或正常早停后加载最佳检查点并生成
`reports/audit/gate_p0a5_train_teacher.json`。只有该报告为`passed`，才能启动Teacher
服务和蒸馏。

恢复逻辑只接受同时包含以下内容的检查点：

- `trainer_state.json`；
- `adapter_model.safetensors`；
- DeepSpeed的`latest`指针及其四卡ZeRO状态。

目录编号更大但缺少训练状态的半成品不得自动恢复。

### 4.3 Student

训练损失权重：

```text
Math 30%
Code 35%
NLP 35%
```

所有训练样本每轮只出现一次，通过样本权重实现任务损失占比，而不是大量复制Math数据。

候选顺序：

1. Candidate 1：Rank 16、Alpha 32、`1e-5`、1轮、Math KL权重0.5。
2. Candidate 2：只有Candidate 1处于78%–82%区间时允许；学习率`7.5e-6`、Math KL权重0.65。

训练完成后：

```text
合并共享LoRA
→ F16 GGUF
→ 训练数据imatrix
→ Q4_K_M
→ Q8_0 KV
→ 唯一300题门禁
```

HF或F16结果不能代替最终Q4_K_M门禁结果。

## 5. 唯一300题门禁

门禁只运行一次数据协议：

```text
Math 100 + Code 100 + NLP 100 = 300
```

Baseline和Student使用完全相同的题目、Prompt、解析器、执行器和生成参数。

```text
领域保持率 = min(Student准确率 / Baseline准确率, 1.0)
```

决策规则：

| 结果 | 决策 |
|---|---|
| 任意领域<78%或生成错误>0 | 拒绝 |
| 三项≥78%，但任意一项<82% | 只允许训练第二个预注册候选 |
| 三项≥82%且截断宏平均≥82% | 推荐进入内存和正式全测 |

78%只是初筛，不代表比赛达标。正式全测仍要求三项和截断宏平均分别≥80%。

## 6. 正式能力测试

正式测试共13,065题：

- GSM8K test：1,319；
- HumanEval：164；
- CMMLU test：11,582。

要求：

- 使用冻结Baseline-14B-AWQ分母；
- Student为实际Q4_K_M、Q8 KV运行配置；
- 三项保持率和截断宏平均均≥80%；
- 生成错误为0；
- 逐题结果密封，只公开任务级准确率和保持率；
- 正式结果不允许反馈到训练。

## 7. 内存与量化

量化模型只有同时通过能力和内存才可冻结为边侧模型。

内存统计包括：

- 模型权重映射；
- Adapter（正式模型应已合并）；
- tokenizer；
- Q8 KV Cache；
- 运行时buffer；
- CPU RSS/PSS和cgroup memory；
- GPU/NPU设备内存。

固定要求：

```text
batch=1
warmup≥20
measure≥100
开发峰值≤1400MB
正式峰值≤1500MB
```

不允许只报告GGUF文件大小，也不允许只监控系统内存而忽略设备内存。

## 8. 云边系统闭环

模型能力只是比赛的一部分。系统继续保留三层路径：

```text
边缘感知
→ 规则/轻量模型Fast Path（目标≤200ms）
→ 低置信复杂任务进入1.7B边缘模型
→ 高风险且网络可用时异步升级14B云端
→ GraphTrust冲突仲裁与KWDB状态同步
```

### 工业场景

- NEU-DET/MVTec AD；
- 边缘缺陷检测、告警、复核和停线动作；
- 云端跨批次分析和模型更新。

### 交通场景

- CityFlow/UA-DETRAC；
- 多摄像头关联、事件去重和交通调度；
- 弱网本地自治与恢复后补传。

两场景共用事件协议、调度器、outbox、数据库和冲突仲裁组件。

## 9. 当前项目状态

截至2026-07-31，状态如下：

| 项目 | 状态 | 证据或说明 |
|---|---|---|
| 14B AWQ正式分母 | 已冻结 | 13,065题，生成错误0 |
| P0-A4旧Student正式结果 | 已判失败并退出主线 | Math/Code/NLP保持率为82.15%/68.57%/70.65% |
| P0-A5数据构建 | 已完成 | 36,673条训练、2,200条内部验证、300题唯一小门禁 |
| 数据隔离 | 已通过 | train/validation/gate互相重叠为0，正式测试训练引用为0 |
| 数据审计 | 已通过 | `reports/audit/gate_p0a5_data.json` |
| 协议审计 | 已通过 | `reports/audit/gate_p0a5_protocol.json` |
| Teacher dry-run | 已通过 | `reports/audit/gate_p0a5_teacher_preflight.json` |
| Student dry-run | 已通过 | `reports/audit/gate_p0a5_student_preflight.json` |
| Teacher正式训练与选择 | 已完成 | 比较`checkpoint-600/800`后冻结选中版本 |
| 蒸馏数据 | 已完成 | 36,673条；Math数值校验、Code执行校验、NLP人工标签锁定 |
| Student Candidate 1 | 已完成 | Rank 16、Alpha 32、`1e-5`、1轮，训练审计通过 |
| Student合并与量化 | 已完成 | Q4_K_M为1,107,408,992字节，Q8 KV运行配置已锁定 |
| 300题门禁v1 | 已失效并归档 | 发现Math提示词要求照抄占位符、NLP解析器漏识别常见有效答案 |
| 300题门禁v2 | Candidate 1失败 | 保持率Math 71.58%、Code 75.86%、NLP 67.09%，宏平均71.51% |
| Candidate 2 | 锁定 | Candidate 1未达到三项78%初筛，不满足第二候选启用条件 |
| 内存与13,065题正式全测 | 禁止启动 | 仅在量化Student三项和宏平均均达到82%推荐线后执行 |

冻结的14B AWQ正式准确率为GSM8K 91.74%、HumanEval 64.02%、CMMLU
75.93%，宏平均77.23%。这些数值继续作为最终保持率分母，P0-A5不得通过降低
Baseline能力来提高保持率。汇总证据为
`reports/audit/gate_p0a4_baseline14b_awq_full.json`。

有效的v2小门禁结果为：

```text
Baseline-14B-AWQ：Math 95%，Code 58%，NLP 79%
Candidate 1 Q4_K_M：Math 68%，Code 44%，NLP 53%
保持率：Math 71.58%，Code 75.86%，NLP 67.09%
截断宏平均：71.51%
生成错误：0
决策：reject
```

v1门禁不是正式失败证据。旧提示词把格式写成`#### <number>`，导致Student照抄
占位符；旧选择题解析器也漏掉“正确答案是D”“故选D”等有效形式。旧trace和审计
均以`v1_invalidated_format`后缀归档。修复后的评测器版本为
`p0a5-gate300-v2`，审计版本为`1.1`，相关单元测试已覆盖占位符和中文选项解析。

## 10. 接下来执行顺序

### 10.1 冻结本轮结果

当前不再训练Candidate 2，不运行内存门禁，也不运行13,065题正式全测。保留：

- Candidate 1训练、合并、imatrix和量化审计；
- v2 Baseline/Student各300行trace和哈希；
- v2保持率审计；
- 失效v1报告及其失效原因。

### 10.2 启动新训练路线前必须解决的问题

本轮不是量化或推理服务故障，主要是共享蒸馏后的能力迁移不足：

1. Math：现有KL只作用于Math样本，Code/NLP批次仍可持续改变共享参数；新路线需要
   全任务基座保持约束，或把Math漂移变成内部验证的硬门槛。
2. Code：44/58，仅差两题达到78%保持线，但不允许围绕门禁逐题调参；需要在
   OpenCodeInstruct内部验证上按可执行通过率选择检查点。
3. NLP：COIG-CQIA内部验证是开放问答，与CMMLU选择题输出形态不一致；新路线需要
   增加不与CMMLU dev/test重叠的中文选择题训练/内部验证来源，COIG继续承担知识
   与中文推理覆盖，不能单独承担选择题迁移验证。
4. Student训练不能再只在整轮结束时记录一次混合`eval_loss`；至少按Math、Code、
   NLP分别记录内部验证指标，并在合并和量化前执行晋级判断。

上述修改属于新的数据/训练协议，必须生成新版本配置、清单和哈希，不得覆盖本轮
P0-A5证据，也不得复用v2门禁逐题错误指导训练。新路线达到独立内部验证标准后，
再注册两个候选并执行新的固定300题门禁。

## 11. 后续并行系统工作

等待长时间Teacher/Student训练和评估期间，应并行补齐比赛系统证据，但不得改动
正在训练的能力数据和协议：

1. 实现边缘规则/轻量模型Fast Path，单独证明两类场景平均端到端时延不超过
   0.2秒；
2. 实现网络质量、任务复杂度和置信度感知的云边调度；
3. 实现弱网本地自治、outbox补传，验证基本业务保持率不低于90%；
4. 实现多边缘节点事件去重、冲突检测和GraphTrust仲裁；
5. 在工业检测与交通监控两个差异场景形成可复现实验；
6. 分别报告模型能力、TTFT、内存、通信量、弱网可用率、冲突率和冲突解决率。

P0-A5模型达到80%能力保持率并不等于作品完成；第8节和本节的系统闭环指标同样是
最终提交和答辩的必要组成部分。

## 12. P0-A6准确率补强路线（当前主线）

P0-A5 Candidate 1 已冻结为失败证据，不再续训，也不从该Adapter继续叠加训练。
P0-A6重新从`models/checkpoints/p0a4/student-shared-merged`启动，当前阶段只处理会
直接提高Math、Code、NLP准确率的问题；量化、内存、时延、弱网和系统闭环暂不进入
本轮训练决策。

### 12.1 训练数据与隔离

P0-A6只读取已有训练/内部验证数据和C-Eval带标签的`val/dev`，不读取C-Eval
`test`、GSM8K test、HumanEval、CMMLU test或任何`data/eval`逐题结果。新数据
写入独立的`data/p0a6`目录，路径、行数和SHA-256由`manifest.json`冻结。

| 领域 | 训练数据 | 有效训练权重 | 内部验证 |
|---|---|---:|---:|
| Math | GSM8K train 7,173条 | 35% | quick 100 / full 200 |
| Code | OpenCodeInstruct严格单函数可执行任务4,088条 | 30% | quick 100 / full 186 |
| NLP | COIG 9,500条 + C-Eval val 1,335条 | 35% | quick 100 / C-Eval dev full 260 |

Code只保留包含明确函数签名、至少三个断言、无顶层执行、无I/O和动态执行的
HumanEval型任务。NLP内部有效权重由COIG开放问答和C-Eval选择题各承担一半；C-Eval
val原始记录没有解释字段内容，因此当前1,335条选择题训练目标实际只包含
`最终答案：A-D`，最终选项token权重为2。C-Eval
val/dev发现的2道重复题已从训练侧删除，训练与完整内部验证ID重叠为0。

### 12.2 Student训练方法

配置文件为`configs/p0a6_accuracy.json`，核心参数固定如下：

- 共享LoRA Rank 16、Alpha 32、Dropout 0.05；
- 学习率`2e-6`，BF16，最大长度1,536，梯度裁剪1.0；
- 4卡DDP，每卡batch 1，梯度累积8，有效全局batch 32；
- 先训练200步，并保留step 100和step 200两个预注册检查点；
- 所有领域、所有rank、每个batch都执行关闭Adapter后的原始基座前向；
- 使用`KL(base||student)`、温度2，Math/Code/NLP的KL权重分别为
  0.60/0.15/0.20，防止共享训练破坏已有Math能力；
- CUDA OOM等运行错误直接写失败审计并退出，不自动降级参数或模型。

### 12.3 检查点选择

训练后在同一个vLLM进程中同时加载原始基座、step 100和step 200两个LoRA，三者
使用完全相同的300题quick内部验证。检查点只有同时满足以下条件才可晋级：

1. 生成错误为0；
2. Math相对原始基座下降不超过1个百分点；
3. Code相对原始基座至少提升3个百分点；
4. NLP相对原始基座至少提升3个百分点。

多个检查点合格时，先选宏准确率最高者，再选步数更早者。只有选中的检查点运行
646题完整内部验证。若两个检查点均不合格，本轮明确失败，停止在内部验证阶段，
不进入量化或正式测试。

### 12.4 当前产物与执行入口

截至2026-07-31，数据构建、全量token扫描、训练器、内部评测器和检查点选择器均已
实现，43项单元测试通过。入口为：

```bash
bash scripts/run_p0a6.sh data-build
bash scripts/run_p0a6.sh preflight
P0A6_GPUS=0,1,2,3 bash scripts/run_p0a6.sh student-train-pilot
bash scripts/run_p0a6.sh validation-auto
```

也可用`bash scripts/run_p0a6.sh pilot-auto`串行完成数据重建、CPU预检、断点续训、
快速评测、检查点选择和完整内部验证。训练完成前不运行正式13,065题测试；完整内部
验证通过后再决定是否注册量化候选和唯一一次正式全量测试。

### 12.5 首轮实测与一次性延长

首轮200步训练已完成。相同quick内部验证的汇总结果如下：

| 模型 | Math | Code | NLP | 结论 |
|---|---:|---:|---:|---|
| 原始基座 | 73% | 49% | 46% | 固定内部增益分母 |
| step 100 | 72% | 50% | 46% | Code、NLP未达到+3个百分点 |
| step 200 | 72% | 52% | 48% | Math、Code通过，NLP少1题 |

三次评测生成错误均为0。首轮选择审计为`failed`，因此没有运行646题完整内部验证。
在不读取逐题错误、不改变训练数据、模型结构或损失权重的前提下，允许且只允许一次
从step 200续训到step 300。新调度在剩余100步使用更低的余弦尾段学习率；只评测
新增step 300，并与已冻结的base/step 200汇总比较。此后无论通过与否均不再延长。

```bash
P0A6_GPUS=0,1,2,3 bash scripts/run_p0a6.sh extension-auto
```

一次性延长实测为Math 74%、Code 50%、NLP 46%，表明继续共享训练会交换领域能力而非
同时提升。延长选择审计同样为`failed`，后续禁止继续增加共享训练步数。

### 12.6 NLP专用Adapter与Top-1路由

当前最佳共享检查点仍是step 200：Math 72%、Code 52%、NLP 48%。为避免NLP更新再次
破坏已经达线的Code，先将step 200合并为稠密共享基座；Math和Code始终直接调用该
基座，只有显式标记为NLP的请求启用专用LoRA，未知任务回退共享基座。

NLP专用LoRA只读取`data/p0a6/train.jsonl`中的10,835条NLP行，Rank/Alpha仍为
16/32，学习率`5e-6`，训练200步并比较step 100/200。关闭Adapter后的合并step-200
基座继续作为KL参考，KL权重0.20；1,335条C-Eval选择题的最终答案token权重从2提高
到4。评测器按领域模型ID路由，因此quick和full报告仍是一个统一三域结果，不允许
手工拼接互不一致的报告。

```bash
P0A6_GPUS=0,1,2,3 bash scripts/run_p0a6.sh nlp-specialist-auto
```

实测结果表明这条开放问答主导的专用Adapter路线失败：step 100的Math/Code/NLP为
71%/52%/45%，step 200为71%/52%/41%，均为零生成错误。相对原始基座46%的NLP
准确率不升反降，`reports/audit/p0a6/router_checkpoint_selection.json`已记录
`failed`，禁止继续延长该Adapter。

### 12.7 选择题专用Adapter实测

为隔离领域更新，第二次实验改用独立Top-1路由：Math直接使用原始v1基座，Code使用
共享step-200 Adapter，NLP使用fresh选择题Adapter。NLP训练只使用C-Eval带标签val
清洗后的1,335题，不再混入COIG开放问答；该split在本项目中明确记为“重新划定的
辅助训练分区”，不是正式CMMLU测试，也不作为本轮评测目标。参数为Rank 16、
Alpha 32、学习率`2e-6`、KL 0.20、答案字母token权重8，训练100步并保留
step 50/100。

固定quick 300题结果如下：

| 路由候选 | Math | Code | NLP | 结论 |
|---|---:|---:|---:|---|
| 原始基座 | 73% | 49% | 46% | 固定增益分母 |
| MCQ step 50 | 72% | 54% | 47% | NLP仅提升1个百分点 |
| MCQ step 100 | 72% | 54% | 44% | NLP下降2个百分点 |

两候选生成错误均为0，Math和Code满足门槛，但NLP均未达到49%的固定晋级线，选择
审计`reports/audit/p0a6/router_mcq_checkpoint_selection.json`为`failed`，因此
没有启动646题验证。该结果说明只有答案字母的监督不能稳定迁移选择题推理能力，
继续增加步数只会过拟合。

### 12.8 下一轮仅保留的准确率修复

下一轮不修改门槛、不续训失败Adapter，只修复训练信号：由冻结14B Teacher针对
1,335条已知人工标签的训练侧选择题生成简短中文理由，人工标签始终是唯一真值；
Teacher若输出不同字母则拒绝，绝不反向覆盖标签。生成结果使用独立trace、SHA-256
和断点续跑，训练目标统一为“简短分析 + 最终答案：A-D”。之后从原始v1基座训练
fresh NLP Adapter，并继续使用Math原基座、Code step-200、NLP专用Adapter的Top-1
路由。只有NLP达到49%且三域门槛全部满足，才运行646题内部验证。

### 12.9 理由后置答案路线实测与失败归因

冻结14B Teacher已完成1,335条C-Eval训练侧选择题的理由生成，全部通过人工标签锁定、
来源隔离和格式校验；`reports/audit/gate_p0a6_mcq_rationales.json`为`passed`，正式
CMMLU测试集读取数为0。基于该数据训练的fresh NLP Adapter保留step 50/100，固定
quick 300题结果如下：

| 路由候选 | Math | Code | NLP | 结论 |
|---|---:|---:|---:|---|
| 原始基座 | 73% | 49% | 46% | 固定增益分母 |
| Rationale step 50 | 73% | 51% | 45% | NLP下降1个百分点 |
| Rationale step 100 | 72% | 53% | 44% | NLP下降2个百分点 |

两候选生成错误均为0，但选择审计
`reports/audit/p0a6/router_rationale_checkpoint_selection.json`为`failed`，未
运行646题验证。训练目标将答案字母放在Teacher理由末尾；Teacher forcing使模型可从
已给出的正确理由预测字母，却没有被直接约束为先根据题目完成选项判断。实测中多数
样本的选项预测没有改变，增加步数还提高了长输出和截断风险。因此该路线冻结为失败
证据，不再续训。

### 12.10 答案优先NLP路线（已预注册，待训练）

新路线只改变会直接影响准确率的训练信号，不改变固定quick/full门槛。训练目标改为：

```text
答案：A
简短理由：不超过两句的训练侧校验理由
最终答案：A
```

首个答案字母位于理由之前，其有效损失权重固定为8；末尾再次给出规范答案以保留推理
格式。训练系统提示词与内部评测器逐字一致。数据由1,335条已验证C-Eval训练侧理由和
公开CMMLU `dev`示例335条组成，共1,670条唯一题目；CMMLU dev训练权重为2。构建
审计`reports/audit/gate_p0a6_answer_first_data.json`为`passed`，标签分布为
A/B/C/D=403/416/411/440，`cmmlu_test_files_opened=0`且
`formal_test_loaded=false`。禁止读取CMMLU test或正式评测逐题结果。

NLP Adapter重新从原始v1基座训练，Rank 16、Alpha 32、学习率`2e-6`、KL权重0.20，
最多50步，只保留预注册的step 25/50两个候选。路由继续固定为Math原始v1、Code共享
step-200、NLP答案优先Adapter。两候选使用同一套quick 300题：Math相对基座下降不
超过1个百分点，Code和NLP分别至少提升3个百分点且生成错误为0。只有候选满足全部
条件，才自动运行646题完整内部验证；不合格则本路线停止，不根据逐题结果修改门槛。

固定执行入口为：

```bash
bash scripts/run_p0a6.sh nlp-answer-first-data
bash scripts/run_p0a6.sh nlp-answer-first-preflight
P0A6_GPUS=0,1,2,3 bash scripts/run_p0a6.sh nlp-answer-first-train
P0A6_SERVE_GPU=0 bash scripts/run_p0a6.sh nlp-answer-first-validation-auto
```

答案优先路线已于2026-07-31完成实测：step 25为Math/Code/NLP=
73%/53%/42%，step 50为72%/53%/41%，生成错误均为0。两候选NLP均低于
原始基座46%，选择审计为`failed`，没有运行646题验证。该路线冻结，不再续训。

## 13. P0-A7 MMLU辅助训练中文选择题路线（当前主线）

P0-A6三种C-Eval小样本路线只改变监督格式，均没有增加足够的新知识。项目历史中的
P0-A4R3审计提供了更强的训练侧证据：3,000条由14B Teacher翻译并独立校验的MMLU
`auxiliary_train`中文选择题，曾在独立256题训练侧验证上把NLP准确率从63.28%提高到
73.83%。当时Code下降来自共享训练；本轮将该数据只用于NLP Top-1 Adapter，Math和
Code路由保持不变。

协议由`configs/p0a7_nlp_specialist.json`冻结。原始官方`data.tar`的SHA-256必须为
`bec563ba4bac1d6aaf04141cd7d1605d7a5ca833e38f994051e818489592989b`，只允许
解压`data/auxiliary_train`。候选请求共4,397条，Teacher输出只有在中文翻译完整、
独立选择与训练标签一致时才接受；最终分层选择3,000条训练和256条不重叠内部验证。
MMLU test、CMMLU test和三项正式测试均禁止读取。

NLP LoRA从原始v1基座重新训练，Rank 16、Alpha 32、学习率`3e-5`、基座KL 0.05，
全局batch 32，训练188步并只保留step 94/188。先在独立256题上选择：生成错误为0，
且相对同提示词基座至少提升3个百分点；多个合格候选取准确率最高者，同分取更早者。
只有选中候选才进入固定quick 300门禁；quick通过后才运行646题完整内部验证。

P0-A7已完成：独立256题准确率由58.20%提升到76.56%，选中step 188；固定quick
300的NLP由46%提升到51%。该组合未晋级的唯一原因是旧Code step-200本次为50%，
相对49%基座仅提升1个百分点，低于预注册的3个百分点。NLP step 188因此冻结，禁止
继续根据quick逐题结果修改。

## 14. P0-A8 OpenCodeInstruct Code专项路线（当前执行阶段）

P0-A8只解决剩余Code瓶颈。为保证最终Top-1 Adapter路由兼容，Code LoRA与已冻结的
P0-A7 NLP LoRA均从`models/checkpoints/p0a4/student-shared-merged`原始v1基座
训练；Math使用无Adapter基座，三类能力不会互相覆盖。协议固定在
`configs/p0a8_code_specialist.json`。

训练只读取`data/p0a6/train.jsonl`中4,088条OpenCodeInstruct Code训练行。
从原646题内部验证中去除已经多次使用的100道Code quick题，得到新的86题独立
Code验证集`data/p0a8/code_internal_validation.jsonl`；它与训练集和旧quick集的
重叠均为0，不读取HumanEval或任何正式测试。

Code LoRA固定Rank 16、Alpha 32、学习率`1e-5`、全局batch 32，训练128步，只允许
step 64/128两个候选。先在86题上要求生成错误为0且相对原始v1 Code至少提升3个
百分点，按准确率最高、同分取更早检查点。选中后只运行一次固定quick 300，路由为
Math原始v1、Code P0-A8候选、NLP P0-A7 step 188；只有Math下降不超过1个百分点、
Code和NLP各提升至少3个百分点时，才自动运行646题内部完整验证。

固定执行入口为：

```bash
bash scripts/run_p0a8.sh data-build
bash scripts/run_p0a8.sh preflight
P0A8_GPUS=0,1,2,3 bash scripts/run_p0a8.sh train
P0A8_SERVE_GPU=0 bash scripts/run_p0a8.sh validation
```

P0-A8已于2026-08-01完成。独立86题Code验证中，原始v1为44/86（51.16%），
旧共享step-200仍为44/86；新step 64为51/86（59.30%），step 128为52/86
（60.47%），两者均通过并按规则选中step 128。固定quick 300结果为Math/Code/NLP=
73%/53%/51%，相对冻结基座为-1/+4/+5个百分点，生成错误为0，因此晋级646题。

646题完整内部验证的清单构成为Code 186、Math 200、NLP 260。原始v1基线为
Math/Code/NLP=76.50%/50.54%/42.69%；最终Top-1路由组合为
77.00%/55.38%/55.00%，分别变化+0.50/+4.84/+12.31个百分点，生成错误为0，
选择审计`reports/audit/p0a6/p0a8_full_selection.json`为`passed`。冻结最终内部候选：
Math使用原始v1，Code使用P0-A8 step 128，NLP使用P0-A7 step 188；不得再根据
这些内部验证的逐题结果调参。

## 15. P0-A9 冻结路由量化前300题能力门

P0-A8内部候选冻结后，先在`data/capability_v2/gate300.jsonl`上运行一次量化前
HF路由验证。该清单固定为GSM8K、OpenCodeInstruct、CMMLU各100题，14B AWQ
基线已封存为Math/Code/NLP=95%/58%/79%。路由固定为Math原始v1、Code P0-A8
step 128、NLP P0-A7 step 188；不得基于本门禁逐题结果继续修改这三个模型。

每项Student/14B保持率均至少78%才进入边侧部署构建；每项和截断宏平均均至少82%
才推荐进行正式全量测试。量化前不满足78%时停止部署转换，先报告能力缺口；满足后
再研究同一基座加两个任务Adapter的llama.cpp部署与Q4_K_M量化，量化模型必须重新
通过同一300题门，HF结果不得替代量化结果。

P0-A9已于2026-08-01完成且未通过。冻结路由HF结果为Math/Code/NLP=
73%/43%/57%，对应14B基线95%/58%/79%的保持率为76.84%/74.14%/72.15%，
截断宏保持率74.38%，生成错误为0。三项均低于78%初筛线，因此不进入量化、内存门
或正式全量测试。本300题结果不得重跑或用于逐题修补；后续若建立第二个最终候选，
只能依据三领域汇总缺口预注册新的train-only数据、训练超参数和独立内部验证集。

## 16. P0-A10 第二个及最终路由候选

P0-A10只使用P0-A9的三领域汇总保持率，不读取其逐题结果。三类Adapter继续从同一个
原始v1基座独立训练，避免互相遗忘。新数据构建固定为：GSM8K 6,917训练+256留出，
OpenCodeInstruct经真实token上限过滤后19,743训练+256留出，NLP使用3,000条MMLU辅助训练中文选择题和
1,335条C-Eval已校验短理由，验证使用Teacher已接受但从未进入P0-A7训练或验证的
256条MMLU辅助训练题。三类训练与验证样本ID重叠为0，禁止打开gate300和正式全测。

Math固定学习率`1e-5`、224步、候选step 112/224，独立验证至少提升2个百分点；
Code使用完整20k来源池而非P0-A8的4,088条窄子集，学习率`5e-6`、256步、候选
step 128/256，至少提升3个百分点；NLP采用MMLU+C-Eval混合选择题，学习率`1e-5`、
136步、候选step 68/136，至少提升3个百分点。公共配置为Rank 16、Alpha 32、
全局batch 32、4 GPU。每类取准确率最高候选，同分取更早检查点。

只有三类全新train-only验证均通过，才允许该第二候选运行一次300题能力门；每项
相对14B保持率至少78%才进入量化，82%为推荐正式全测线。P0-A10是最后一个允许
进入300题能力门的训练候选，失败后不得通过重复测试或第三候选继续试探该清单。

P0-A10已于2026-08-01完成四卡训练和全新train-only验证。Math原始v1基座为
78.52%，step 112/224分别为71.09%/67.19%，两候选均造成明显退化；Code基座为
46.48%，step 128/256分别为48.44%/48.83%，虽提升1.95/2.34个百分点，但未达到
预注册的3个百分点门槛；NLP基座为66.02%，step 68/136分别为82.03%/82.81%，
step 136以16.80个百分点的增益通过并被选中。Math、Code选择状态为`failed`，
NLP为`passed`，因此最终300题能力门保持阻止，未发生第三次门禁试探。

NLP首轮报告曾因评测器只接受“最终答案：X”、未接受验证清单明确要求的`FINAL: X`
而产生系统性误判。解析器现同时接受两种末行格式并新增回归测试；原始生成响应未
重新调用GPU，而是以确定性重评分生成v2审计，旧报告完整归档到
`reports/audit/p0a10/parser_v1/`。该修复只纠正评分协议，不改变模型输出、验证题目
或训练数据。P0-A10冻结结论是：保留原始v1 Math，NLP step 136可作为新的有效资产，
Code step 256仅作为正向但未达门槛的证据；不得据此降低既定门槛或启动第三候选。

## 17. P0-A11 安全准确率修复（2026-08-01重新授权）

用户在明确知晓P0-A9/P0-A10结果后，重新授权只依赖训练侧证据的安全优化；该授权
不允许重读P0-A9逐题输出、正式全测反馈、降低门槛或覆盖既有审计。P0-A10 NLP
step 136冻结为路由资产，不再训练。P0-A11只修复距离门槛最近的Math与Code，所有
训练由4张独立GPU执行并受60%系统内存守护；旧模型、旧报告和密封结果均保留。

Math从原始v1基座开始。先用基座推理从未用于历次检查点选择的GSM8K训练行，训练集
内部挖掘答错题，并加入数量为错题50%的答对题回放；另冻结300题全新train-only
验证。LoRA为Rank 8、Alpha 16、学习率`1e-6`、KL 0.20、128步，只比较step 64/128，
相对同验证基座至少提升2个百分点才通过。

Code也从原始v1基座开始。OpenCodeInstruct只保留短Python函数、10项独立执行测试
全部通过、无文件/网络/第三方依赖且完整训练序列不超过1536 token的任务，固定选择
16,000条真正不同的训练题；验证从从未参与历次选优的train-only内部池冻结。LoRA为
Rank 16、Alpha 32、学习率`3e-6`、KL 0.02、500步，只比较step 250/500，相对基座
至少提升3个百分点才通过。此设计修正P0-A10只训练0.41 epoch和KL约束过强的问题。

只有Math和Code新验证均通过、NLP step 136审计仍完整且三者生成错误为0，才允许把
三者组成一个Top-1路由，并在冻结300题上运行一次新的P0-A11候选。每项相对14B保持率
至少78%才进入合并、Q4_K_M量化和内存验证，82%为推荐正式全测线。P0-A11若未通过，
只保存训练侧汇总、损失和验证汇总用于下一轮数据/优化方法设计，不读取门禁逐题错误，
也不重复该候选的300题测试。

### 17.1 P0-A11实测结论

P0-A11已完成。Math全新300题中，原始v1为236/300（78.67%），step 64为
239/300（79.67%，+1个百分点），step 128为235/300（78.33%）；未达到预注册的
+2个百分点，说明基座错题训练约一轮时达到峰值，第二轮已经退化。Code全新814题中，
原始v1为368/814（45.21%），step 250为413/814（50.74%，+5.53个百分点），
step 500为409/814（50.25%，+5.04个百分点）；两候选均通过并冻结step 250。
所有评测生成错误为0。因Math未通过，P0-A11没有打开300题能力门。

## 18. P0-A12 MetaMath Math专项修复

P0-A12只修复Math；Code固定使用P0-A11 step 250，NLP固定使用P0-A10 step 136，
两者不再训练。由于本地GSM8K train已经全部参与过训练、挖掘或历次内部验证，新候选
不得复用P0-A11的300题选优。新独立验证冻结为SVAMP官方1,000题，SVAMP不进入训练，
只允许查看基座和两个候选的汇总准确率，要求零生成错误且相对基座至少提升2个百分点。

Math从原始v1基座重新训练。数据固定为MetaMathQA提交
`aa4f34d3d2d3231299b5b03d9b3e5a20da45aa18`中16,000条GSM型增强题，只接受
`GSM_Rephrased`、`GSM_AnsAug`和`GSM_SV`，统一为末行`#### 数值`并执行去重、
历史验证原题排除和1536-token扫描；再加入P0-A11训练侧1,424道基座错题和712道
正确回放。配置为Rank 16、Alpha 32、学习率`2e-6`、KL 0.15、全局batch 32、
576步，只比较step 288/576。四卡训练继续受60%系统内存守护。

只有SVAMP选择通过且冻结Code/NLP审计完整，才允许P0-A12 Top-1路由运行一次300题
能力门；每项保持率至少78%才进入量化与内存验证，82%为推荐正式全测线。失败时不得
复用SVAMP逐题错误或降低门槛。

### 18.1 P0-A12实测结论

P0-A12已完成四卡576步训练，step 288和step 576检查点、优化器状态及训练审计均
完整，系统内存守卫未触发。独立SVAMP 1,000题结果为：原始v1基座841/1000
（84.10%），step 288为825/1000（82.50%），step 576为823/1000（82.30%），
生成错误均为0。虽然两候选的规范答案格式率由基座97.6%提高到99%以上，但实际
答题准确率分别下降1.6和1.8个百分点，说明MetaMath长理由SFT主要强化了输出格式，
没有改善独立算术推理泛化。选择审计为`failed`，P0-A12没有打开300题能力门。

SVAMP逐题输出禁止用于后续调参；冻结P0-A12为失败证据，不再续训或降低+2个百分点
门槛。Math继续使用未受损的原始v1基座，Code继续冻结P0-A11 step 250，NLP继续
冻结P0-A10 step 136。

## 19. P0-A13 数学原生推理运行时优化

P0-A9汇总显示Math只比78%保持率线少约2道题，而P0-A10至P0-A12连续三次Math
专项SFT均未产生可靠迁移。P0-A13因此不再修改模型权重，只比较同一原始v1 Math
基座在Qwen3原生推理模式关闭和开启时的能力。Code step 250与NLP step 136保持
冻结，数学请求独占推理模式，Code/NLP继续关闭推理，避免改变已通过的训练侧资产。

新的运行时选择集固定为`EleutherAI/asdiv`提交
`8f95807222d87b4c688c3c22a6ba2801e1fa03e2`。原始2,305题只保留2,238道答案为
有限十进制数的题目，确定性去除1道重复题后冻结为2,237题；分数、百分数和类别答案
排除，以保持与GSM8K数值评分器一致。
ASDiv不进入任何训练，并检查其与P0-A12 Math训练提示词精确重叠为0。该数据只比较
一个预注册候选：同模型、同提示词、temperature 0、max tokens 768，唯一变量为
`enable_thinking=false/true`。

只有开启推理相对关闭推理至少提升2个百分点、规范答案格式率至少95%、生成错误为0，
才运行一次P0-A13冻结300题能力门。门禁路由固定为Math原始v1+thinking、Code
P0-A11 step 250、NLP P0-A10 step 136；Math token上限768，Code 768，NLP 256。
每项相对14B保持率至少78%才进入Q4_K_M量化和内存验证，82%为推荐正式全测线。
若ASDiv不通过，则保持300题门关闭，不复用ASDiv或SVAMP逐题结果设计下一候选。

### 19.1 P0-A13实测结论

ASDiv 2,237题完整结果为：关闭推理1,929题正确（86.23%），开启推理1,958题正确
（87.53%），净增29题、1.30个百分点，生成错误均为0。该提升是真实正增益，但低于
预注册的2个百分点门槛，`reports/audit/p0a13/runtime_selection.json`为`failed`，
P0-A13没有打开300题能力门。后续只允许使用这一汇总结果，不读取ASDiv逐题错误；
推理模式作为有价值但单独不足的运行时资产保留。

## 20. P0-A14 数学三样本自一致性

P0-A14继续保持原始v1 Math权重不变，在开启Qwen3原生推理的基础上验证固定三样本
自一致性。候选一次生成3个答案，temperature固定0.6、top-p固定0.95、每题种子由
样本ID确定；评分前只提取三个数值答案并按多数票选择，三者不同则确定性选择第一个。
算法不能读取参考答案来选响应。基线为同模型开启推理、temperature 0的单样本输出，
两者使用相同提示词和768 token上限。

新的选择集冻结为`MU-NLPC/Calc-mawps`提交
`38c10053efeafd20ab6ff4e08c3ec17de26c19b7`中的validation 1,040题。为保持与
GSM8K数值评分器一致，排除306道分数答案，再确定性排除与P0-A13 ASDiv精确重叠的
7题，最终保留727题；与P0-A12 Math训练提示精确重叠为0。
MAWPS不进入训练，也不允许复用ASDiv/SVAMP进行P0-A14选优。

只有自一致性相对单样本基线至少提升2个百分点、规范答案率至少95%、生成错误为0，
才运行一次P0-A14能力门。能力门Math使用同一三样本投票，Code固定P0-A11 step
250，NLP固定P0-A10 step 136。自一致性会提高数学计算耗时，因此报告必须同时给出
平均延迟倍率；它不增加模型权重或固定KV内存，但最终仍须单独满足边侧内存门。

### 20.1 P0-A14实测结论

Calc-MAWPS 727题结果为：开启推理的单样本基线594题正确（81.71%），三样本投票
589题正确（81.02%），下降5题、0.69个百分点；三样本平均延迟为单样本的1.66倍，
生成错误为0。选择审计为`failed`，300题能力门保持关闭。该结果说明随机采样增加的
答案多样性没有形成可靠共识，后续不再调整温度、top-p、样本数或投票规则。

## 21. P0-A15 轻量Math Adapter增量缩放

P0-A11 Math step 64是历次Math训练中唯一在独立GSM型留出上出现正增益的检查点
（+1个百分点），但完整增量未达到原协议+2%门槛。P0-A15不再训练，只比较该冻结
Adapter的0.5倍增量与原始1.0倍增量，并在Math请求上统一开启Qwen3推理。0.5倍通过
将LoRA alpha由16冻结为8实现，权重不改；1.0倍直接使用原step 64。最多两个候选，
Code step 250和NLP step 136不变。

新的选择集使用`MU-NLPC/Calc-mawps`同一固定提交中从未接触的test 520题，排除170道
分数答案和4道与P0-A12至P0-A14验证重叠的十进制题，最终冻结346题；这些题不训练。
基线为原始v1+thinking，候选仅改变Math Adapter增量，同提示词、temperature 0、
max tokens 768。候选相对基线至少提升2个百分点、规范格式率至少95%、生成错误为0
才可晋级；多个合格候选按准确率最高、同分优先0.5倍选择。

### 21.1 P0-A15实测结论

Calc-MAWPS test 346题中，原始v1+thinking基线为292/346（84.39%），0.5倍step64
仍为292/346，1.0倍step64为296/346（85.55%）。完整增量带来4题、1.16个百分点
的正增益，但仍低于预注册2个百分点，选择审计为`failed`且300题门保持关闭。0.5倍
没有效果，后续淘汰；1.0倍作为第二项独立正向证据保留。

## 22. P0-A16 Math联合运行时候选

P0-A13证明原生推理相对关闭推理提高1.30个百分点；P0-A15证明完整step64在开启推理
的基座上再提高1.16个百分点。P0-A16只验证这两个冻结改动的联合效果，不增加新训练、
新超参数或更多候选：基线固定为原始v1且thinking关闭，候选固定为P0-A11 step64
完整增量且thinking开启。两者使用同提示词、temperature 0和max tokens 768。

验证集使用Calc-MAWPS固定提交中从未接触且未训练的train分片1,089题；排除45道分数
答案及3道与P0-A12至P0-A15验证重叠的十进制题，最终冻结1,041题。联合候选相对
基线仍须至少提升2个百分点、规范格式率至少95%、生成错误为0才可进入一次P0-A16
能力门，门槛没有降低。能力门固定路由为Math step64+thinking、Code step250、NLP
step136；失败则不再围绕Math推理、采样或Adapter缩放继续试探。

### 22.1 P0-A16实测结论与Math冻结

Calc-MAWPS train 1,041题中，原始v1关闭推理为951/1,041（91.35%），step64开启
推理为921/1,041（88.47%），下降2.88个百分点，选择审计为`failed`，300题未运行。
由此确认Math运行时改动高度依赖数据分布，不再围绕推理、采样、Adapter或缩放试探。

重新核对已有且不重跑的正式全量汇总后，旧Q4边侧模型在完整GSM8K上的保持率已经为
82.15%，满足比赛80%下限；HumanEval和CMMLU保持率仅68.57%和70.65%。因此后续
Math直接冻结为原始Q4基座，真正瓶颈是Code和NLP。该判断只读取正式全量任务级汇总，
不读取逐题结果，也不再次运行完整测试。

## 23. P0-A17 Code/NLP专项能力门

P0-A17只验证已通过独立训练侧选择的Code step250与NLP step136，不再包含Math题。
能力门从冻结gate300中只读取Code和NLP各100题，14B基线分别为58%和79%；候选只
运行一次，生成错误必须为0且两项保持率均至少78%。Math使用既有正式全量保持率
82.15%作为冻结证据，不用小样本重复否定完整数据结果。

P0-A17通过后进入边侧部署验证：原始Q4_K_M基座、Q8 KV、按任务Top-1选择一个LoRA；
量化/部署模型必须重新验证Code/NLP门和1.5GB内存，随后才允许新的最终路由执行一次
完整官方测试。P0-A17失败则根据Code/NLP任务级汇总重新训练相应领域，不触碰Math。

### 23.1 P0-A17实测结论

P0-A17一次性Code/NLP能力门结果为：Code候选42/100、14B基线58/100，保持率
72.41%；NLP候选62/100、14B基线79/100，保持率78.48%；生成错误为0。结合既有
完整GSM8K保持率82.15%，Math与NLP均已达到当前78%小规模晋级线，只有Code未通过。
因此冻结Math原始Q4路由与NLP step136，后续训练和选择只处理Code。

## 24. P0-A18 已有Code Adapter跨数据集迁移审计

P0-A11 Code step250曾在同源OpenCodeInstruct留出集上提高，但P0-A17未达标。
P0-A18不训练新权重，先使用固定提交
`google-research-datasets/mbpp@4bb6404fdc6cacfda99d4ac4205087b89d32030c`
的full/validation 90题比较原始1.7B基座、step250和step500。MBPP三条公开断言作为
函数接口规范放入提示词，并由隔离执行器重新执行；与P0-A11训练及历史验证的精确提示
重叠均为0。候选相对基座至少提高3个百分点、规范代码块率不得低于基座、生成错误为0
才可晋级。选择器只读取任务级审计，不读取逐题输出来设计后续候选。

### 24.1 P0-A18实测结论

独立MBPP 90题中，原始基座48题正确（53.33%），step250为38题（42.22%），
step500为40题（44.44%）；两个旧Adapter分别下降11.11和8.89个百分点，选择审计
为`failed`。这证明旧Adapter对同源合成数据的收益没有迁移到短函数任务，不能继续
量化或进入正式门。P0-A18验证题不进入后续训练，其逐题结果也不用于调参。

## 25. P0-A19 MBPP与OpenCodeInstruct混合Code蒸馏

P0-A19从原始共享1.7B基座重新训练Code Adapter，不继承P0-A11 Code增量。训练集
固定为8,000条从P0-A11执行验证语料中按完整token长度最短确定性选择的函数任务，以及
MBPP full/train中373条可执行参考任务；1道与独立验证提示重叠的MBPP训练题被剔除。
MBPP训练权重8、OpenCode权重1，统一KL保持权重0.05。LoRA固定Rank16、Alpha32、
学习率1e-6、256步，仅保存并评测step128和step256两个候选。

新的选择集为同一固定提交的MBPP sanitized/test。原始257题中剔除1道与P0-A18
提示重叠题和1道重复提示，冻结255题；与P0-A19训练提示精确重叠为0，参考代码不写入
评测清单。候选相对同一基座至少提高3个百分点、规范格式率不低于基座、生成错误为0
才可晋级。Math与NLP全程冻结，不读取HumanEval或完整测试数据。训练使用4 GPU、
有效全局batch 32、系统RAM 60%停止守卫和step128断点。

### 25.1 P0-A19实测结论

P0-A19四卡训练与step128/256检查点均完整。首次验证服务因2048 token验证请求超过
1536 token服务上下文而返回HTTP 400；修复仅将验证服务上下文提高到2048，并以原始
追踪与哈希生成独立基础设施修复审计，未改变模型、题目、答案或选择门槛。恢复后的
MBPP sanitized/test 255题结果为：原始基座148/255（58.04%），step128与step256
均为150/255（58.82%），仅增加2题、0.78个百分点，生成错误均为0。两候选低于
预注册的+3个百分点，选择审计为`failed`；不得量化或进入能力门。该结果说明重复
提高少量MBPP训练题权重只能改善输出格式，不能形成足够的独立执行迁移。

## 26. P0-A20 Code原生推理运行时审计

P0-A20不训练新权重，只检查Qwen3原生thinking能否修复代码推理。验证集使用MBPP
中未被P0-A18/P0-A19训练或验证接触的剩余239题；基线为原始1.7B、thinking关闭，
候选仅允许原始基座thinking开启与P0-A19 step256 thinking开启。相同任务、提示、
执行器和768 token上限，候选相对基线至少提升3个百分点且规范格式不得退化才通过。

### 26.1 P0-A20实测结论

239题中，原始基座关闭thinking为101/239（42.26%）；原始基座开启thinking仅
2/239（0.84%），P0-A19 step256开启thinking为74/239（30.96%）。两个thinking
候选规范代码块率均为0，选择审计为`failed`。原因是当前Qwen3服务的推理输出格式
与短函数代码协议不兼容，且在固定token预算内挤占最终代码。后续Code固定thinking
关闭，不再围绕推理开关调参。

## 27. P0-A21 全新可执行OpenCode训练路线

P0-A18至P0-A20证明历史OpenCode留出、少量MBPP重采样和thinking均不足。P0-A21
从原始共享1.7B基座重新训练，不继承任何旧Code Adapter。数据固定使用
`nvidia/OpenCodeInstruct@8f3ba5bafe4d6e8db46082cf7ae6741bc370604d`的三个
本地冻结分片，共扫描300,000行；先排除21,100个历次已使用ID、非短函数任务、
依赖网络/文件/第三方库和不可在1536 token内训练的样本，再由隔离执行器逐题执行
10项测试。最终7,000题全部通过独立执行，冻结6,000题训练、1,000题验证，二者ID
重叠为0；验证提示仅公开3项接口测试，评分仍执行全部10项测试。

HumanEval在本阶段仅用于自动n-gram污染排除，未读取答案、测试或历史结果。训练配置
固定为Rank16、Alpha32、学习率1.5e-6、KL 0.08、有效全局batch32、384步，仅保存
step192与step384；四卡训练受60%系统RAM守卫保护。独立1,000题选择要求：候选相对
同一原始基座至少提高3个百分点、规范代码率不低于基座、生成错误为0。只有选择通过，
才允许入选检查点运行一次冻结Code100能力门；保持率至少78%才进入Q4_K_M量化、
Q8 KV和1.5GB边侧内存验证。未通过时不降低门槛、不读取逐题错误指导训练，也不打开
正式HumanEval。

### 27.1 P0-A21实测结论

P0-A21四卡384步训练成功，step192/384断点与60%系统RAM守卫审计完整。全新
OpenCode 1,000题结果为：原始基座612/1000（61.20%），step192为622/1000
（62.20%，+1.0个百分点），step384为634/1000（63.40%，+2.2个百分点）；
三个模型生成错误均为0，两个Adapter规范代码率均超过99%。step384有持续正增益，
但仍比预注册+3个百分点线少8题，因此P0-A21选择为`failed`，没有打开Code100。

## 28. P0-A23 全新数据续训

P0-A21的step192至step384仍在提升，但原阶段余弦学习率已经衰减至接近0。P0-A23
因此冻结step384为初始化Adapter，使用全新优化器和完全不重叠的新数据继续一轮；
不是从零训练，也不读取P0-A21逐题结果。第二批数据再次从相同冻结OpenCode提交中
筛选，排除全部28,100个历史ID后独立执行7,581个候选，冻结6,000训练题与1,000
验证题，所有目标题均通过10项测试，历史/训练/验证ID重叠均为0。

续训保持Rank16、Alpha32与KL 0.08，学习率降至8e-7，固定192步，仅比较step96和
step192。新验证同时测原始基座、P0-A21 step384初始资产和两个续训候选；候选必须
相对原始基座至少+3个百分点、不低于初始资产、规范代码率至少99%且生成错误为0。

### 28.1 P0-A23实测结论

四卡续训192步成功。全新1,000题中，原始基座593题正确（59.30%），P0-A21
step384初始资产628题（62.80%，+3.5个百分点），P0-A23 step96与step192也均为
628题。结果证明P0-A21增益可以迁移到第二套独立留出，但普通随机新题续训已进入平台；
同分按更早、更温和检查点冻结step96。P0-A23选择审计为`passed`。

## 29. P0-A24 一次性Code100能力门

P0-A24只运行选中的P0-A23 step96，复用冻结Code100与同题14B基线；不重复Math/NLP
推理，只读取既有完整Math和P0-A17 NLP任务级证据。Code候选至少需要46/100，才能
达到14B基线58/100的78%保持率。门禁最多运行一次，失败不得重跑或查看逐题错误调参。

### 29.1 P0-A24实测结论

Candidate最终45/100，14B为58/100，Code保持率77.586%；Math完整保持率82.149%，
NLP冻结保持率78.481%，生成错误为0。Code仅差1题但仍严格判定`failed`，不四舍五入、
不降低门槛、不重复同一Code100。该结果与两套OpenCode独立验证共同说明：执行验证
数据方向有效，但随机正确解SFT已到平台，下一阶段必须改为当前Student的train-only
失败样本挖掘与正确回放，而非继续增加同分布随机题或训练轮数。

## 30. P0-A25 train-only失败挖掘与回放续训

P0-A25冻结P0-A23 step96为初始化Adapter，另取与全部历史训练、验证和能力门都不重叠
的OpenCodeInstruct任务：6,000题只用于失败挖掘、1,000题用于新的独立验证，另冻结
100题作为此前从未运行的新能力门。所有7,100题均通过10项独立执行测试，HumanEval
和历次Code100均不进入数据选择、训练或错误分析。

初始Student在6,000题train-only挖掘池中答对4,001题、失败1,999题，生成错误为0。
训练语料包含全部1,999个失败样本和确定性抽取的1,000个正确回放样本；失败样本权重2、
正确回放权重1，KL保持权重0.10。续训固定Rank16、Alpha32、学习率6e-7、有效全局
batch32与192步，仅保存step96和step192。选择标准同时要求相对原始基座至少提升3个
百分点、相对初始化Adapter至少提升1个百分点、规范代码率至少99%、生成错误为0。

### 30.1 P0-A25实测结论

全新1,000题中，原始基座611题正确，初始化Adapter 638题，step96为645题，step192
为652题。step192相对原始基座提升4.1个百分点、相对初始化提升1.4个百分点，两个
预注册门槛均通过，因此冻结
`models/checkpoints/p0a25/code-failure-repair/checkpoint-192`。这证明当前有效增益来自
对Student真实失败的定向修复与正确样本回放，而不是重复随机扩充或增加训练轮数。

## 31. P0-A26 全新Code100能力门

P0-A26使用P0-A25阶段预先冻结且此前从未推理的100题。14B AWQ以4 GPU仅运行一次，
得到81/100的固定分母；P0-A25 step192以相同提示词、评分器和执行测试运行一次，得到
68/100，Code保持率83.9506%，超过78%初筛线与82%推荐全测线，生成错误为0。旧
P0-A24的逐题追踪没有用于本阶段训练或选优，正式HumanEval也保持关闭。

## 32. P0-A27 Q4_K_M边缘部署验证

部署使用已冻结的1.1GB Q4_K_M共享基座、Q8 KV、thinking关闭和P0-A25 Code LoRA。
LoRA由原始safetensors转换为F16 GGUF，未修改训练权重；llama.cpp使用同一模型基座
动态加载，避免复制一份完整领域模型。量化部署形态在同一P0-A26 Code100上得到
67/100，相对14B的保持率82.7160%，相对HF候选保留98.5294%，生成错误为0，因此
量化能力门通过。

GPU全层卸载配置的进程RSS约799MB、设备内存约1,609MB，按CPU与设备内存合计口径
为约2,408MB，明确判定不满足1.5GB，不作为达标证据。边缘内存档改为CPU推理、
`ctx=512`、8线程、batch 1、Q8 KV与关闭主机提示缓存；长上下文代码任务由调度器升级
云端。该档须完成20次预热和100次正式测量，CPU、设备与运行时总峰值不得超过1500MB。
1400MB开发线的动态LoRA启动值约1408MB，属于开发余量不足但不改变1500MB正式
硬门；正式内存审计通过前不得宣布边侧模型最终冻结。

### 32.1 P0-A27正式内存结论

正式边缘内存档完成20次预热和100次连续测量，进程树与设备内存合计峰值为
1419.85MB，RSS的P95为1419.82MB，生成进程未出现持续增长或错误，正式审计状态为
`passed`。因此Code路由已经同时满足量化能力保持率82.72%和单次推理内存不超过
1.5GB两项硬要求，冻结Q4_K_M基座、F16 Code LoRA、Q8 KV、thinking关闭和
`ctx=512`的边缘配置；不再继续训练或压缩Code权重。GPU全卸载档仅作为服务器性能
对照，不能作为1.5GB边缘达标配置。

## 33. P0-A30/P0-A31 NLP缩放诊断

P0-A30不训练新权重，仅在与冻结NLP100不重叠的CMMLU dev剩余235题上比较P0-A10
step136 Adapter的LoRA缩放0.75、1.0和1.25。三者分别为149/235、145/235和
145/235，因而按预注册内部选择冻结0.75缩放。但该缩放在一次性冻结NLP100门上仅
得到53/100，14B固定分母为79/100，保持率67.09%，明显低于原始1.0缩放已经取得的
62/100和78.48%。这说明235题上的4题差异不能稳定外推，属于采样噪声或分布偏差。

因此撤销0.75缩放的晋级资格，保留原始step136、scale=1.0为当前最佳NLP资产；不再
对同一Adapter做缩放或提示词搜索，也不使用冻结门逐题结果调参。NLP的下一次候选必须
来自全新的train-only数据与训练，而不是后处理参数。

## 34. P0-A32 新NLP数据驱动续训

P0-A32是NLP方向第二个也是最后一个训练候选。数据仅取MMLU `auxiliary_train`，排除
4,397个历史Teacher请求身份后，按六个来源和学科类别确定性构造6,075个新请求；与
历史请求精确重叠为0，不使用MMLU test、CMMLU test或冻结NLP100。14B AWQ Teacher
通过4 GPU张量并行生成中文短理由和最终选项，只有Teacher选项与训练标签一致的样本
才能进入数据集，目标冻结4,000题训练与500题内部验证，并记录来源覆盖和内容哈希。

新训练从当前最佳P0-A10 NLP step136初始化，使用低学习率、Math能力保持约束和旧NLP
高质量样本回放，最多保存两个预注册检查点。只在新的500题train-only内部验证上选择；
相对初始Adapter至少提高2个百分点且生成错误为0才允许打开一次新的冻结NLP100门。
NLP100至少需要65/100，才达到相对14B 79/100的82%推荐线。若第二个训练候选仍未
达到该线，则保留原始62/100结果并结束模型侧NLP调参，转向云边路由的系统能力报告，
不得继续反复使用冻结小门或正式CMMLU全量结果选优。

## 35. P0-A42单轮复训与P0-A43边侧正式全量结论

P0-A42按预注册方案分别训练Math、Code和NLP领域LoRA，并只在train-only内部验证上
选择。Math原始基座、step108、step216分别为199/256、196/256、198/256；Code历史
P0-A25 step192、本轮step96、本轮step192分别为655/1000、649/1000、652/1000；
NLP历史P0-A10 step136、本轮step50、本轮step100分别为144/260、144/260、141/260。
同分优先稳定旧资产，因此最终路由冻结为Math共享基座、Code P0-A25 step192、NLP
P0-A10 step136。本轮新权重没有替换历史最佳资产。

P0-A43将上述路由部署为真实边侧格式：1.1GB Q4_K_M共享基座、Math无Adapter、Code
F16 GGUF LoRA、NLP F16 GGUF LoRA、Q8 KV和thinking关闭。能力全测以4 GPU仅作加速，
按领域独立服务并使用与密封14B AWQ分母完全相同的v15提示、评分器和13,065个样本；
逐题提示哈希差异、遗漏、重复和生成错误均为0。最终结果为GSM8K 999/1319
（75.7392%）、HumanEval 0/164、CMMLU 5451/11582（47.0644%）。相对14B的正式
保持率分别为82.5620%、0%和61.9854%，截断宏保持率48.1825%，因此正式全量结论为
`failed`。HumanEval结果表明OpenCode内部验证收益没有迁移到正式函数补全协议；
CMMLU也未保持训练内选择题收益。该全量结果只允许任务级汇报，不得读取逐题输出继续
训练、选择提示词或重跑同一候选。

## 36. P0-A44任务形态对齐复训与一次新边侧全量

P0-A44针对P0-A43暴露的迁移断裂进行一次新的数据驱动复训，不读取P0-A43正式逐题
输出。Math正式保持率82.56%已达标，固定使用无Adapter的Q4_K_M共享基座，不再训练。
Code汇总P0-A21、P0-A23和P0-A25三个互不重复的OpenCode执行验证池，共18,000个原始
任务；只保留能够确定单一目标函数、转换为HumanEval v15“仅函数体”协议后再次通过
全部10项单元测试的样本。最终冻结11,351题训练、1,000题独立验证，训练和验证身份
重叠为0；3,941个在格式转换后语义不再等价的任务被拒绝，不能进入训练。

NLP使用与CMMLU v15一致的中文四选一提示和单字母目标：C-Eval人工标签1,335题、
中文化且标签校验的MMLU辅助题1,000题、COIG-CQIA人工标注单选题1,605题，共3,939题。
选择只使用C-Eval dev 260题与CMMLU dev 335题，CMMLU test 11,582题不进入训练、
提示选择或错误分析。两个领域均从同一P0-A4共享HF基座独立训练Rank16、Alpha32、
学习率1e-6、KL 0.12的LoRA；Code运行近一轮352步并保留step176/352，NLP运行128步
并保留step64/128。每个领域只比较两个预注册候选，且候选必须在聚合内部验证上不低于
基座，否则该领域回退共享基座。

HF选择后将Adapter转换为F16 GGUF，并在1.1GB Q4_K_M共享基座、Q8 KV、thinking关闭
的真实部署形态重新验证；量化候选低于量化基座时再次回退。最终冻结路由后才允许执行
一次新的13,065题边侧全量，沿用密封14B AWQ分母、v15提示和同一评分器。全量结果只
形成任务级准确率、保持率、哈希与生成错误汇总，无论通过或失败均不得反馈到后续训练。

### 36.1 P0-A44实测结论

Code与NLP四卡训练均成功，两个预注册检查点完整。HF内部验证中，Code共享基座、
step176、step352分别为603/1000、604/1000、572/1000，选择step176；NLP共享基座
在C-Eval dev与CMMLU dev合计350/595，step64为353/595且两套验证均不退化，选择
step64，step128回落至346/595。两个HF候选均完成F16 GGUF转换。

真实Q4_K_M部署复核发现量化后存在明显负迁移：Code Adapter为467/1000，低于Q4
基座596/1000；NLP Adapter为332/595，低于Q4基座334/595且C-Eval少2题。因此按
冻结规则两个领域均回退Q4共享基座。llama.cpp在thinking关闭时仍残留空的
`<think></think>`正文包装，首次Q4内部评分被判为无效并可恢复地移入
`reports/audit/p0a44/invalidated_reasoning_envelope_20260803`；统一移除该传输包装后
重新评分，未改变模型、题目、生成参数或参考答案。

最终一次13,065题边侧全量使用Q4_K_M共享基座、Q8 KV与thinking关闭，结果为GSM8K
999/1319（75.7392%）、HumanEval 75/164（45.7317%）、CMMLU 6170/11582
（53.2723%）。相对14B AWQ固定分母1210/105/8794的保持率分别为82.5620%、
71.4286%和70.1615%，截断宏保持率74.7173%，因此状态为`failed`。13,065个样本
无遗漏、重复、提示哈希差异或生成错误；结果已密封，禁止据此继续训练或调参。

## 37. P0-A45简化共享训练与唯一300题测试

P0-A45不再使用独立领域Adapter、路由、MoE、选项概率评分、多候选或KL损失，只训练
一个共享LoRA。基础模型固定为P0-A4共享HF模型；训练数据由GSM8K训练蒸馏数据、
20,000条执行验证后的OpenCodeInstruct任务，以及去重后的中文四选一短理由数据组成。
三类监督损失质量比例固定为20%/30%/50%，Rank16、Alpha32、学习率1e-6、训练一轮。
Math通过监督回放保护，不再设置单独的Math训练阶段。

训练结束后将唯一LoRA合并进HF权重，再使用三类训练数据生成imatrix并重新量化为
Q4_K_M；边缘运行固定Q8 KV、thinking关闭。量化模型只运行一次固定300题测试，组成
为Math、Code、NLP各100题，并与既有14B AWQ固定分母计算保持率。78%为初筛线，
82%为推荐全测线，生成错误必须为0。本阶段不读取P0-A44密封全量逐题结果，也不通过
300题结果生成第二个候选。

P0-A45已于2026年8月3日完成。实际训练集为37,351条：GSM8K 7,173条、
OpenCodeInstruct 20,000条、中文选择题10,178条；内部验证集1,795条。四卡训练完成
1,167步，训练损失6.3981，内部验证损失0.4958，随后完成合并、训练数据imatrix和
Q4_K_M量化，最终模型大小1,107,408,960字节。固定300题测试得到Math 63/100、
Code 39/100、NLP 44/100，生成错误为0；14B AWQ固定分母为95/100、58/100、
79/100，对应保持率66.3158%、67.2414%、55.6962%，截断宏保持率63.0845%。
该版本未达到78%初筛线，判定为`reject`，不进入全量测试，也不据此自动生成第二候选。

## 38. P0-A46单域NLP隔离修复

P0-A45证明，将数学推理、函数代码和中文选择题三种差异很大的监督目标同时写入一个
共享LoRA，即使学习率仅为1e-6，也会产生显著跨任务干扰。内部同源验证损失不能替代
生成式能力验证；同时，直接量化后再测试无法区分训练损伤和量化损伤。下一轮据此冻结
三项约束：不继承P0-A45权重、不再三域联合训练、HF验证通过前不量化。

P0-A46回退P0-A4共享HF基座，只训练一个NLP专用LoRA；Math继续使用冻结共享基座，
Code继续使用冻结P0-A25资产，因此NLP训练不能修改另外两项权重。训练集固定为P0-A45
已经审计的10,178条14B校验中文选择题，统一为与能力门一致的“短分析+最终答案字母”
协议；Rank16、Alpha32、学习率4e-7、KL保持0.05、答案token权重2.0，共318步，
只保留step159和step318。

检查点选择只使用训练外C-Eval dev 260题和CMMLU dev 335题。候选必须在两套验证上
均不低于同一HF基座，且合计至少增加6题；否则本轮训练失败并停止。只有HF选择通过，
才允许转换F16 GGUF并在Q4_K_M基座上复核；Q4候选须两套均不退化且合计至少增加3题，
才允许运行一次固定300题。此顺序用于明确区分训练泛化、LoRA部署和量化三个环节。

P0-A46已于2026年8月3日完成四卡训练，共318步、约一轮，用时25分02秒；step159和
step318均完整保存，训练与系统内存守卫正常。HF训练外验证中，共享基座在C-Eval与
CMMLU dev分别答对143/260、206/335，合计349/595；step159为143/260、207/335，
合计350/595；step318为144/260、206/335，合计同为350/595。两个检查点都只增加1题，
低于预注册的至少增加6题要求，因此选择审计为`failed`。该结果说明单域隔离避免了
P0-A45的灾难性跨任务退化，但现有10,178条选择题蒸馏数据仍只能改善输出契约，无法
稳定增加训练外知识。P0-A46不转换GGUF、不量化，也不运行固定300题。

## 39. P0-A47/P0-A48最多两轮边侧全量

按用户授权，后续最多执行两次“训练+边侧正式全量”，无论内部验证是否达到推荐增益，
每轮均完成真实Q4_K_M、Q8 KV、thinking关闭的13,065题全量。第一轮P0-A47只修复
Code：冻结P0-A25 step192，在P0-A44已经通过10项执行验证且转换成HumanEval v15
函数体协议的11,351条train-only任务上重新推理，训练集只取Student失败题和最多
3,000条正确回放；Rank16、Alpha32、学习率3e-7、256步，比较step128/256并选择
较强的新检查点。即使未达到内部+20题推荐线，也转换为F16 GGUF并完成第一轮边侧全量。

第一轮全量只允许读取GSM8K、HumanEval、CMMLU三项汇总正确率和保持率，不读取逐题
错误指导训练。若任一领域或截断宏保持率未达到80%，再依据汇总短板执行且仅执行一次
P0-A48训练；第二轮完成相同Q4边侧全量后无条件停止，不产生第三候选。两轮均复用固定
14B AWQ分母、13,065个样本身份、v15提示和评分器，逐题结果密封，不进入Git。

P0-A47已完成。train-only错误扫描中，P0-A25 Code初始Adapter在11,351题上答对
6,612题，课程由4,739条失败题和2,369条确定性正确回放组成。四卡训练完成256步；
独立1,000题HF验证中初始、step128、step256分别为618、616、613题，按预注册规则
选择较强的新step128。Q4部署验证中，新Adapter为584/1000，低于Q4基座596/1000。
仍按授权完成第一轮全量，结果为GSM8K 999/1319、HumanEval 75/164、CMMLU
6170/11582；相对固定14B分母的保持率为82.5620%、71.4286%、70.1615%，截断宏
保持率74.7173%，生成错误0，状态`failed`。Code正式结果与P0-A44基座完全相同，
说明继续在同一HumanEval形态训练池上做低学习率失败续训没有获得新的正式迁移收益。

最终第二轮P0-A48据此冻结Math与Code，不再重复训练；只修复同样未达标且样本规模更大
的NLP。训练输入固定为P0-A44的3,939条CMMLU-v15同形态、单字母目标train-only题，
先由裸Student扫描，再使用全部错误题与一半数量的确定性正确回放。训练固定Rank16、
Alpha32、学习率5e-7、KL 0.10、128步，只比较step64/128；HF与Q4选择均只读取
C-Eval dev 260题和CMMLU dev 335题聚合结果。Q4内部选择较强路线后，无论收益多少
均完成第二次13,065题边侧全量；该全量完成后无条件终止模型训练。

P0-A48已完成并触发两轮上限。裸Student在3,939条train-only选择题中答对2,399题，
课程由1,540条失败题与770条正确回放组成；四卡Rank16训练完成128步，用时约10分08秒，
无OOM。HF训练外验证中裸基座为351/595，step64为352/595，step128为350/595，选择
step64；转换F16 GGUF后，真实Q4验证中裸基座334/595、候选332/595，因此按最佳路线
回退裸Q4。第二轮全量再次得到GSM8K 999/1319、HumanEval 75/164、CMMLU
6170/11582，对应保持率82.5620%、71.4286%、70.1615%，截断宏74.7173%，生成错误0，
状态`failed`。这证明低学习率错误重采样只带来HF内部+1题的微小波动，无法穿过Q4部署；
当前主要限制是1.7B基座的代码与中文学科知识容量，而非继续调LoRA学习率或重复同一
训练池。按预注册约束，P0-A48后停止模型训练与正式全量，不生成P0-A49候选。
