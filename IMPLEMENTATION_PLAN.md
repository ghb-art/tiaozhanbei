# DB4AI-EdgeServe P0-A5 实施计划

## 1. 项目目标

题目编号：XH-202606
题目名称：面向云边协同场景的分布式人工智能感知与决策关键技术研究

本项目建设“边缘实时感知与自治、云边协同推理、全局一致性决策”完整系统，并以可复现证据达到以下指标：

| 指标 | 正式要求 | 项目开发目标 |
|---|---:|---:|
| Math/Code/NLP能力保持率 | 每项80%–90% | 小门禁推荐线82%，正式每项≥80% |
| TTFT降低 | ≥75% | ≥78% |
| 单次推理内存 | ≤1500MB | ≤1400MB |
| 弱网业务保持率 | ≥90% | ≥92% |
| 两类场景平均端到端时延 | ≤200ms | ≤180ms |
| 决策冲突比例 | ≤5% | ≤4% |
| 冲突解决成功率 | ≥90% | ≥92% |

能力、内存、时延和系统一致性必须分别提供证据。任何开发门通过都不得写成正式比赛指标已经通过。

## 2. 当前有效模型角色

### 2.1 Baseline-14B-AWQ

- 模型：Qwen2.5-14B-Instruct-AWQ。
- 角色：冻结的云端工程基线和能力保持率分母。
- 禁止训练、禁止根据正式逐题错误调整提示词或训练数据。
- 已完成正式13,065题评测，结果与逐题记录继续封存。
- 报告同时补充BF16 Teacher的小规模一致性结果，解释AWQ作为工程基线的合理性。

### 2.2 Distill-Teacher-14B

- 模型：Qwen2.5-14B-Instruct BF16。
- 训练：4 GPU、DeepSpeed ZeRO-3、LoRA Rank 16、Alpha 32、学习率`1e-5`、1轮。
- CPU参数和优化器卸载继续禁用。
- 作用：生成和校验Math、Code、NLP训练信号，不作为正式分母。

### 2.3 Edge-Student

- 基座：当前表现最稳定的Qwen3-1.7B v1 HF合并模型。
- 方法：单一共享多任务LoRA，不再使用三个任务Adapter。
- 默认参数：Rank 16、Alpha 32、学习率`1e-5`、1轮。
- 最多两个预注册候选；第二候选只能降低学习率并加强Math保持约束。
- 最终量化：Q4_K_M权重、Q8_0 KV Cache、纯训练数据imatrix。

## 3. 唯一有效的能力数据协议

旧96题、170题、MBPP、APPS、CodeContests、MMLU翻译和任务Adapter协议全部退出主线。历史结果只作为失败路线证据，不参与新候选选择。

### 3.1 数据总表

| 领域 | 训练 | 训练内部验证 | 唯一小门禁 | 正式测试 |
|---|---|---|---|---|
| Math | GSM8K train 7,173题 | GSM8K train留出200题 | GSM8K train固定100题 | GSM8K test 1,319题 |
| Code | OpenCodeInstruct筛选20,000题 | 同源独立1,000题 | 同源独立100题 | HumanEval 164题 |
| NLP | COIG-CQIA分层9,500题 | 同源独立1,000题 | CMMLU dev固定100题 | CMMLU test 11,582题 |

内部验证只用于训练损失和检查点选择，不是能力门禁。正式测试不进入训练、蒸馏、内部验证、提示词选择或候选选择。

### 3.2 Math

- GSM8K官方train按照固定种子划分为7,173/200/100。
- 训练保留人工推理过程和最终数值答案。
- 数值答案通过`####`后的标准答案自动校验。
- GSM8K test 1,319题完全封存。

Math已接近达标，训练目标是防遗忘。共享Student的Math损失权重固定为30%，并对Math答案Token计算“启用LoRA模型”与“禁用LoRA的v1基座”之间的KL保持损失：

```text
Math loss = 0.5 × supervised loss + 0.5 × base-student KL
```

候选的Math保持率相对当前v1不得下降超过1个百分点。

### 3.3 Code

来源固定为`nvidia/OpenCodeInstruct`。只保留：

- Python短函数；
- 明确函数签名；
- 字符串、数组、字典、集合、排序、搜索、计数、基础贪心和动态规划；
- 完整单元测试；
- 数据集报告测试全部通过且平均测试分为1.0；
- 本项目独立沙箱再次执行通过；
- 与HumanEval不存在精确或高相似题面重叠。

排除：

- 网络、文件、GUI、数据库、外部服务和大型第三方依赖；
- 超长上下文和工程脚手架；
- 无法执行验证的开放式任务；
- 重复题面、仅改变量名或模板生成的重复任务。

Code正式测试仍为HumanEval 164题，执行时使用隔离进程、单候选pass@1和隐藏单元测试。可使用EvalPlus执行器强化同一164题的测试，但不得把HumanEval题目加入训练。

### 3.4 NLP

来源固定为`m-a-p/COIG-CQIA`。上游44,694条中只有12,726条标记
`human_verified=true`；执行长度、安全、去重、去CMMLU重叠和目标领域筛选后，
可用的不同样本为10,602条。因此不以未人工核验样本凑足20,000条，而是固定
9,500条训练、1,000条内部验证并保留102条安全余量。样本按互斥的学科优先级
确定性分层，避免一个多标签样本被重复计入多个类别：

| 类别 | 数量 |
|---|---:|
| 考试与试题 | 300 |
| 百科与自然科学 | 1,700 |
| 历史、文化与社会科学 | 1,240 |
| 法律、经济与管理 | 670 |
| 语言理解与逻辑推理 | 5,590 |

所有样本必须`human_verified=true`。排除代码、纯数学、闲聊、营销、主观创作、来源不清、超长样本和CMMLU重叠题。

Teacher只生成1–3句简短分析，人工核验的原答案保持不变：

```text
简短分析：……
最终答案：人工核验的参考答案
```

CMMLU dev固定100题覆盖全部67个学科，用于唯一小门禁；CMMLU test 11,582题只用于正式测试。

### 3.5 数据许可与提交

- OpenCodeInstruct：CC BY 4.0，记录数据版本和来源。
- COIG-CQIA数据卡当前未给出完整许可证结论；原始数据、筛选数据和逐题输出不进Git。
- Git只提交下载、筛选、去重、执行验证、哈希和重建脚本。
- 作品材料记录来源、使用范围和哈希；不擅自重新分发上游原始数据。

## 4. 训练流程

### 4.1 CPU准备

```text
下载3个分散的OpenCodeInstruct分片和COIG-CQIA
→ 构建三个训练集
→ 独立执行21,100个Code候选
→ 生成2,200条训练内部验证
→ 生成固定300题门禁
→ 数据重叠、数量、哈希和协议审计
→ Teacher/Student训练dry-run
```

入口：

```bash
bash scripts/run_p0a5.sh data-download
bash scripts/run_p0a5.sh data-build
bash scripts/run_p0a5.sh preflight
```

### 4.2 Teacher

```text
BF16 14B + 4 GPU ZeRO-3
→ 共享LoRA训练1轮
→ 启动4 GPU张量并行Teacher端点
→ 生成Math/Code/NLP蒸馏答案
→ Math数值校验、Code执行校验、NLP人工参考答案锁定
```

Teacher生成失败时，只允许回退到已经独立验证的原训练答案，并在审计中记录；不得接受未验证的生成内容。

### 4.3 Student

训练损失权重：

```text
Math 30%
Code 35%
NLP 35%
```

所有训练样本每轮只出现一次，通过样本权重实现任务损失占比，而不是大量复制Math数据。

候选顺序：

1. Candidate 1：Rank 16、Alpha 32、`1e-5`、1轮、Math KL权重0.5。
2. Candidate 2：只有Candidate 1处于78%–82%区间时允许；学习率`7.5e-6`、Math KL权重0.65。

训练完成后：

```text
合并共享LoRA
→ F16 GGUF
→ 训练数据imatrix
→ Q4_K_M
→ Q8_0 KV
→ 唯一300题门禁
```

HF或F16结果不能代替最终Q4_K_M门禁结果。

## 5. 唯一300题门禁

门禁只运行一次数据协议：

```text
Math 100 + Code 100 + NLP 100 = 300
```

Baseline和Student使用完全相同的题目、Prompt、解析器、执行器和生成参数。

```text
领域保持率 = min(Student准确率 / Baseline准确率, 1.0)
```

决策规则：

| 结果 | 决策 |
|---|---|
| 任意领域<78%或生成错误>0 | 拒绝 |
| 三项≥78%，但任意一项<82% | 只允许训练第二个预注册候选 |
| 三项≥82%且截断宏平均≥82% | 推荐进入内存和正式全测 |

78%只是初筛，不代表比赛达标。正式全测仍要求三项和截断宏平均分别≥80%。

## 6. 正式能力测试

正式测试共13,065题：

- GSM8K test：1,319；
- HumanEval：164；
- CMMLU test：11,582。

要求：

- 使用冻结Baseline-14B-AWQ分母；
- Student为实际Q4_K_M、Q8 KV运行配置；
- 三项保持率和截断宏平均均≥80%；
- 生成错误为0；
- 逐题结果密封，只公开任务级准确率和保持率；
- 正式结果不允许反馈到训练。

## 7. 内存与量化

量化模型只有同时通过能力和内存才可冻结为边侧模型。

内存统计包括：

- 模型权重映射；
- Adapter（正式模型应已合并）；
- tokenizer；
- Q8 KV Cache；
- 运行时buffer；
- CPU RSS/PSS和cgroup memory；
- GPU/NPU设备内存。

固定要求：

```text
batch=1
warmup≥20
measure≥100
开发峰值≤1400MB
正式峰值≤1500MB
```

不允许只报告GGUF文件大小，也不允许只监控系统内存而忽略设备内存。

## 8. 云边系统闭环

模型能力只是比赛的一部分。系统继续保留三层路径：

```text
边缘感知
→ 规则/轻量模型Fast Path（目标≤200ms）
→ 低置信复杂任务进入1.7B边缘模型
→ 高风险且网络可用时异步升级14B云端
→ GraphTrust冲突仲裁与KWDB状态同步
```

### 工业场景

- NEU-DET/MVTec AD；
- 边缘缺陷检测、告警、复核和停线动作；
- 云端跨批次分析和模型更新。

### 交通场景

- CityFlow/UA-DETRAC；
- 多摄像头关联、事件去重和交通调度；
- 弱网本地自治与恢复后补传。

两场景共用事件协议、调度器、outbox、数据库和冲突仲裁组件。

## 9. 当前阶段退出条件

当前只推进到GPU训练前：

1. 新数据下载和CPU构建完成；
2. 旧96/170、MBPP/APPS/CodeContests/MMLU训练协议退出主线；
3. `gate_p0a5_data.json`通过；
4. `gate_p0a5_protocol.json`通过；
5. Teacher和Student dry-run通过；
6. 下一步由用户在终端明确启动4 GPU Teacher训练。

在上述条件未满足前，不启动GPU训练。
